# 1-2 提示词工程基础

提示词（Prompt）是你和大模型之间唯一的“接口”。写得好，实习生如虎添翼；写得烂，它自由发挥。本章给你一套能直接上手的写法，不讲玄学。

## 一、提示词的四个基本构件

1. **角色（Role）**：告诉模型“你是谁”。例：“你是一名资深后端工程师。”
2. **任务（Task）**：明确要它做什么。例：“把下面这段日志按错误等级分类。”
3. **上下文（Context）**：必要的背景、格式、约束。
4. **示例（Few-shot）**：给 1~3 个输入输出样例，模型照着学最快。

## 二、一个能直接抄的模板

```
你是一名{角色}。
背景：{上下文}
任务：{具体任务}
要求：
1. 只输出 JSON，字段为 {字段定义}
2. 不要解释、不要多余文字
3. 如果信息不足，字段值填 null
输入：
{用户内容}
```

把花括号换成具体内容，比“帮我写个东西”强十倍。

## 三、常见技巧（都验证过有用）

- **结构化输出**：明确要求“只输出 JSON / 只输出 YAML”，下游才好解析（对应第 14 章输出解析器）。
- **分步思考**：加一句“请一步步思考，再给结论”，复杂推理准确率明显提升。
- **约束边界**：明确“不允许编造引用”“数据不足就写未知”，能压住幻觉。
- **角色锚定**：让模型稳定在某个专业视角，减少跑题。

## 四、一个对比示例

❌ 差提示词：

> 帮我总结一下这篇文档。

✅ 好提示词：

```
你是一名技术文档编辑。
任务：把下面的产品文档压缩成 5 条要点。
要求：每条不超过 30 字；用中文；只列事实，不评价。
文档：
{文档正文}
```

## 五、提示词的边界

提示词解决不了三件事：模型知识截止前的实时信息（用 RAG）、精确计算（用代码工具）、私有数据（用 RAG/工具）。当你发现“怎么写提示词都不准”，多半是撞到了这三堵墙——这时该上工具，而不是继续调词。

## 下一步

下一章 [1-3 RAG、微调与智能体选型](1-3-RAG、微调与智能体选型.md) 帮你判断：遇到“不准”到底该用哪种技术补。
