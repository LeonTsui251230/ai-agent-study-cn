# 1-1 大模型认知与工程概览

很多人把“大模型”“AI”“Agent”混为一谈。本章先把这几件事说清楚，再给一张工程落地的全景图，让你知道后面 27 章各自处在什么位置。

## 一、三个常被混淆的词

- **大模型（LLM）**：一个被训练来“预测下一个 token”的神经网络。它本身不会“做事”，只会“接话”。ChatGPT、通义千问、Llama 都是大模型。
- **应用（Application）**：把大模型包一层界面和逻辑，比如一个聊天网页。它仍然是对话式的。
- **智能体（Agent）**：让大模型能够**主动调用工具、读写环境、循环决策**去完成目标。它不止“回答”，而是“去办”。

一句话区分：大模型是大脑，应用是带壳的脑袋，Agent 是“大脑 + 手脚 + 决策循环”。

## 二、大模型能做什么、不能做什么

**能做**：文本生成、摘要、翻译、分类、一定程度的推理、按格式输出、调用你给的工具。

**不能做（至少不靠谱）**：实时获取最新信息（除非接搜索）、精确计算大数、访问私有数据（除非接 RAG）、保证事实正确（会幻觉）、执行真实动作（除非接工具）。

理解了“不能做”的那一半，你就理解了为什么需要 RAG、工具调用、Agent——它们都是给大模型“补短板”。

## 三、一次 Agent 调用的内部发生了什么

```
用户目标
  → 大模型理解意图，决定调用哪个工具
  → 执行工具（查数据库 / 搜网页 / 跑代码）
  → 把结果喂回大模型
  → 大模型判断：够了就回答，不够就再调工具
  → 输出最终结果
```

这个“思考 → 行动 → 观察 → 再思考”的循环，就是 Agent 的骨架（ReAct 模式）。后面的 LangChain Agent、LangGraph 都是把这个骨架工程化。

## 四、工程落地全景图

```
            ┌─────────────┐
            │   你的业务   │
            └──────┬──────┘
                   │
        ┌──────────┼──────────────────────┐
        │          │                       │
   知识层(RAG)  能力层(工具)          编排层(Agent)
   │              │                       │
 向量库        搜索/API/代码          LangChain/LangGraph
        │          │                       │
        └──────────┼──────────────────────┘
                   │
            ┌──────┴──────┐
            │  模型接入层  │  Ollama / 云API / 私有部署
            └─────────────┘
```

这张图对应了本教程的分段：知识层（2、19 章）、能力层（17、20 章）、编排层（21~26 章）、模型接入层（11、12、7 章）。

## 五、给新手的三条建议

1. **先跑通再理解**：第 10 章的 Hello World 先抄一遍，有了体感再回头啃原理。
2. **小步快跑**：别一上来就做“全自动公司”。先做一个“能查知识库就回答问题”的最小可用版本。
3. **把大模型当实习生**：能力上限高、下限也低，必须给清晰指令、给工具、要它产出可校验的结果。

## 下一步

下一章 [1-2 提示词工程基础](1-2-提示词工程基础.md) 讲怎么给这位“实习生”写清楚指令。
