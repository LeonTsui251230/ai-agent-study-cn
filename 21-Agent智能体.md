# 21 Agent 智能体原理

前面每章都是“一个能力”。Agent 把这些能力用“循环决策”串起来自主完成任务。本章讲清 Agent 的心智模型。

## 一、Agent = 模型 + 工具 + 循环

```
while 没结束:
    思考 = 模型(目标, 历史, 可用工具)
    if 思考说“够了”: 输出答案, 结束
    else: 执行思考选中的工具 → 把结果加入历史
```

这个“思考—行动—观察”的循环，经典叫 **ReAct**（Reason + Act）。

## 二、为什么需要循环

单轮“提示词→回答”只能处理一步。真实任务往往要：先查资料、再算、再查、最后总结。Agent 靠循环把多步串起来，中间还能根据上一步结果调整下一步。

## 三、Agent 的关键组成

- **规划（Planning）**：把大目标拆成子任务（可用“先把任务列出来”的提示词实现）。
- **记忆（Memory）**：短期装当前循环历史，长期装跨会话偏好（第 16 章）。
- **工具（Tools）**：让它能办事（第 17 章）。
- **反思（Reflection）**：做完自我检查“这答案对吗、要不要重做”。

## 四、Agent 的风险

- **失控循环**：模型一直调工具停不下来 → 要设最大步数。
- **错误累积**：某步工具结果错，后面全歪 → 要可观测、可中断。
- **成本失控**：每一步都调模型，长任务 token 烧得快 → 要预算上限。

## 下一步

单 Agent 不够时，用图来编排更复杂的流程——[22 LangGraph 概述与入门](22-LangGraph概述与快速入门.md)。
