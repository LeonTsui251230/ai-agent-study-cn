# 全书术语表

按首字母/拼音排序，方便查阅。

- **Agent（智能体）**：能主动调工具、循环决策完成目标的系统。
- **A2A（Agent-to-Agent）**：Agent 之间互相通信协作的协议思路。
- **Chain（链）**：把多个组件按顺序串起来的处理流程。
- **Embedding（嵌入）**：把文本变成向量，使语义相近的文本向量也相近。
- **Fine-tuning（微调）**：用标注数据调整模型权重，使其贴合特定任务。
- **LangChain**：主流 LLM 应用框架，组件化拼接能力。
- **LangGraph**：基于“图”的 Agent 编排框架，支持状态、分支、循环。
- **LCEL**：LangChain 表达式语言，用 `|` 串联组件。
- **LLM（大语言模型）**：被训练来预测下一个 token 的大模型。
- **MCP（模型上下文协议）**：统一模型连接外部工具/数据的开放协议。
- **Ollama**：本机运行开源大模型的工具。
- **Prompt（提示词）**：你发给模型的指令文本。
- **RAG（检索增强生成）**：先检索相关资料再让模型基于资料回答。
- **Reducer**：LangGraph 里合并节点输出的函数（如追加消息）。
- **State（状态）**：LangGraph 节点间传递的共享数据。
- **Tool（工具）**：Agent 可调用的函数，用于弥补模型不能执行的动作。
- **Token**：模型处理文本的最小单位，计费与长度都按它算。
- **Vibe Coding**：用自然语言让 AI 帮你生成代码的开发方式。
- **Vector Store（向量库）**：存储向量并支持相似检索的数据库。
- **vLLM**：高性能大模型推理服务框架。
