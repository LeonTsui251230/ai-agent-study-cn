# 新手入门与常见问题

## 一、我完全是小白，从哪开始？

严格按编号走：先 1-1 建立认知，再 1-2 学提示词，然后 2、3 章用平台做出第一个 Bot。不要跳到 LangChain —— 先有体感，代码才好懂。

## 二、一定要会 Python 吗？

- 只做原型/低代码 Bot：**不需要**，Coze/Dify 拖拽即可（第 3 章）。
- 要做到生产级、自定义逻辑：**需要**基础 Python。本教程代码都是入门级，照抄能跑。

## 三、没有 GPU 能学吗？

能。用云端大模型 API 或本机 Ollama 跑 7B 级别小模型（第 12 章），普通笔记本足够。微调才需要显卡，而微调是最后一章、且多数场景用不到。

## 四、调用大模型要花钱吗？

- 学 LangChain 配 Ollama 本地模型：**零成本**。
- 用云端 API：有按量计费，但开发期用量极小，几块钱能玩很久。把 `max_tokens` 设小、用完关掉不必要的请求即可控成本。

## 五、为什么我照抄代码跑不通？

按这个顺序排查：
1. 模型 Key / 本地服务（Ollama）有没有起。
2. 包版本对不对（`pip install` 时看报错）。
3. 网络能否访问对应服务。
4. 把报错原文贴给模型，让它帮你定位（这也是练 Agent 思维）。

## 六、RAG 和微调选哪个？

见 [1-3 选型](1-3-RAG、微调与智能体选型.md)。一句话：知识用 RAG，风格用提示词，只有“模型能力本身要变”才微调。

## 七、Agent 会不会失控乱花钱？

会，如果没设限。务必加：最大步数、工具超时、token 预算、敏感操作人工确认（第 21、24 章）。

## 八、学完能找工作吗？

本教程给你**能力**，不承诺**岗位**。把六个实战项目亲手做一遍、能讲清每个技术选型的原因，你就具备了真实的工程判断力——这比背八股有用。
