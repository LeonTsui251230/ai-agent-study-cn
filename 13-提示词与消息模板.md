# 13 提示词与消息模板

在代码里用字符串拼接提示词，既难维护又容易出错。本章讲 LangChain 的提示词模板，怎么把“结构”和“变量”分开。

## 一、为什么要模板

```python
# 反例：拼接脆弱、易注入、难改
prompt = "你是一名" + role + "，请回答：" + question
```

模板把“固定结构”和“动态变量”分开，可读、可复用、可防注入。

## 二、ChatPromptTemplate（最常用）

```python
from langchain_core.prompts import ChatPromptTemplate

prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一名{role}，只依据给定资料回答。"),
    ("human", "资料：{context}
问题：{question}"),
])

print(prompt.format_messages(
    role="客服助理",
    context="退货周期为7天。",
    question="能退吗？",
))
```

## 三、Few-shot 示例模板

需要给模型看样例时，用 `FewShotPromptTemplate` 或把示例直接写进消息列表：

```python
prompt = ChatPromptTemplate.from_messages([
    ("system", "把中文地址转成结构化 JSON。"),
    ("human", "北京市海淀区中关村大街1号"),
    ("ai", '{"city":"北京","district":"海淀","street":"中关村大街","num":"1号"}'),
    ("human", "{address}"),
])
```

## 四、模板的最佳实践

- **系统提示词固定**：角色与约束写死在 system 消息。
- **变量只放内容**：context、question 这类动态数据用占位符。
- **别把用户原文当提示词指令**：避免“提示词注入”（用户写“忽略上面的规则”）。

## 下一步

模型吐出的可能是 JSON 字符串，怎么稳定解析？[14 输出解析器](14-输出解析器.md)。
