# 7 企业级大模型服务部署

当你要把模型能力提供给整个团队或客户，就不能只在笔记本上 `python app.py` 了。本章讲企业级部署的几个关键点：算力、服务封装、并发、鉴权、监控。

## 一、部署的两种形态

- **推理服务（API）**：把模型包成一个 HTTP 接口（如 OpenAI 兼容接口），业务系统来调。
- **应用服务**：把“模型 + 提示词 + 工具 + 界面”整体部署成一个网站/后台。

多数企业先有前者，再有后者。

## 二、推理服务怎么起（以 vLLM 为例）

```bash
# 假设已准备好模型权重
python -m vllm.entrypoints.openai.api_server   --model Qwen/Qwen2.5-7B-Instruct   --host 0.0.0.0 --port 8000
```

起好后，业务代码用 OpenAI 兼容客户端就能调，无需关心背后是哪家模型。

## 三、企业级必须考虑的四件事

1. **并发与排队**：突发流量要排队、限流，避免把显存打爆。
2. **鉴权**：接口必须带 token，且按业务隔离配额。
3. **可观测**：记录每次调用的延迟、token 消耗、成功率，方便算成本和定位慢请求。
4. **回滚**：模型/提示词更新出问题时，能快速切回上一版。

## 四、显存与成本

7B 模型量化后约 4~6GB 显存，70B 需要多卡。部署前先用 `显存 ≈ 参数量 × 每参数字节数` 粗估，再留 20% 余量。成本大头通常是 GPU 时长和调用次数，上线前一定要压测。

## 下一步

如果暂时没有 GPU 服务器，先用 Docker 把环境标准化——[8 Docker 入门与开源项目部署](8-Docker快速入门与开源项目部署排障.md)。
