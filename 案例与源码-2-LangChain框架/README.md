# 案例：LangChain 框架

对应正文第四篇（第 9~19 章），给出可直接运行的示例代码。

## 目录

- `rag_min.py`：最小 RAG 链（第 19 章）。
- `tool_call.py`：工具调用示例（第 17 章）。
- `memory_redis.py`：用 Redis 存对话历史（第 16 章）。

运行前 `pip install -r ../../requirements.txt` 并配置 `.env`。
