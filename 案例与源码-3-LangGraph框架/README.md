# 案例：LangGraph 框架

对应正文第五篇（第 20~26 章），给出单 Agent 与多智能体示例。

## 目录

- `single_agent.py`：ReAct 循环的单 Agent（第 21~23 章）。
- `multi_agent.py`：supervisor 多智能体（第 26 章）。

运行前 `pip install -r ../../requirements.txt` 并配置 `.env`。
