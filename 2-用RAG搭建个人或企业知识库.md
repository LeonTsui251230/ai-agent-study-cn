# 2 用 RAG 搭建个人/企业知识库

RAG（Retrieval-Augmented Generation，检索增强生成）是落地最快、性价比最高的 AI 应用形态。本章讲清楚原理，并带你搭一个最小可用的知识库。

## 一、RAG 在解决什么

大模型训练数据有截止日期、不含你的私有资料。RAG 的做法是：用户提问时，先从你的资料里**检索出相关片段**，拼进提示词，再让模型基于这些片段回答。这样模型“临时读到了”你的资料，幻觉大幅减少。

## 二、RAG 的五个环节

```
文档 → 切片 → 向量化(Embedding) → 存入向量库
                                ↓
用户问题 → 向量化 → 在库里检索相似片段 → 拼进提示词 → 模型回答
```

1. **切片（Chunking）**：把长文档切成 200~500 字的小块，太碎丢上下文、太大引入噪声。
2. **向量化**：用 Embedding 模型把每段文字变成一串数字（向量），语义相近的文字向量也相近。
3. **存储**：向量存进向量数据库（第 18 章细讲）。
4. **检索**：把问题也向量化，找最近的几段。
5. **生成**：把检索到的片段 + 问题一起丢给大模型。

## 三、最小可运行示例（伪代码）

```python
# 1. 建库
chunks = split_document("制度.pdf")
vectors = embed(chunks)
db.add(vectors, chunks)

# 2. 问答
question = "差旅报销上限是多少？"
hits = db.search(embed(question), top_k=3)
prompt = f"根据资料回答：{hits}
问题：{question}"
answer = llm(prompt)
```

具体实现见第 18、19 章（向量库与 RAG 实战）。

## 四、三个容易翻车的点

- **切片策略**：按段落切比按固定字数切效果好；带标题的块优于孤立块。
- **检索质量**：检索错了，模型再强也答不对。先用关键词/向量混合检索提升召回。
- **来源可追溯**：生产环境一定要把“答案来自哪段”返回给用户，便于核对，也方便你发现检索问题。

## 下一步

不想写代码也能做 RAG——下一章 [3 基于 Coze / Dify 的智能体开发](3-基于Coze和Dify平台的智能体开发.md) 用低代码平台快速搭出来。
