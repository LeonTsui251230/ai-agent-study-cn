# 6 Coze 与 Dify 的部署

本节讲清楚“部署”这两类平台意味着什么，以及当你有数据驻留、内网访问、定制需求时该怎么选。

## 一、云端 SaaS vs 私有部署

- **云端（Coze / Dify 云端）**：零运维，注册即用，但数据要出内网。
- **私有部署（主要是 Dify 开源版）**：数据留在自己服务器，可深度定制，但要自己运维。

选型的硬约束通常来自合规：涉及客户隐私、合同、内部制度的资料，多数企业要求私有部署或至少数据不出域。

## 二、Dify 私有部署的最小形态

Dify 官方提供 `docker-compose` 一键起：

```bash
git clone https://github.com/langgenius/dify.git
cd dify/docker
cp .env.example .env
docker compose up -d
```

起来后访问 `http://<服务器IP>:80` 即可。细节排障见第 8 章。

## 三、Coze 的本地化选项

Coze 以云端为主；若要在本地跑类似能力，常见做法是：用开源框架（如 Dify、FastGPT）替代，或把模型换成私有模型（Ollama，见第 12 章），在前端接 Coze 的协议。

## 四、部署时要决定的几件事

1. **模型放哪**：用云端大模型 API，还是本地 Ollama，还是私有推理服务？
2. **数据落哪**：知识库向量存在哪台机器、是否加密。
3. **访问控谁**：谁能用这个 Bot、谁能改提示词。
4. **备份怎么搞**：知识库和配置定期备份，避免“改坏无法回滚”。

## 下一步

无论哪种部署，最后都会落到“模型服务怎么跑起来”——[7 企业级大模型服务部署](7-企业级大模型服务部署.md) 接着讲。
