# 28 大模型微调概述与整体流程

提示词和 RAG 解决不了“模型不会这么说话/不会这种格式”时，才会考虑微调。本章给你全貌，不求你立刻上手，但求你分得清“要不要做”。

## 一、微调在补什么

预训练模型是“通用大脑”。微调是用**你的一批标注数据**，让它更贴合某个任务或风格。它改的是模型权重，不是提示词。

## 二、三种常见微调

| 方式 | 改动量 | 成本 | 适用 |
| --- | --- | --- | --- |
| 全量微调 | 全部权重 | 极高 | 基座训练 |
| LoRA / QLoRA | 只训小部分 | 中 | 最常见，单卡可跑 |
| 指令微调（SFT） | 用问答对 | 中 | 让模型学会按指令/格式产出 |

## 三、整体流程

```
1. 明确目标：到底要模型“变”成什么样
2. 准备数据：高质量问答对/样本（质量 >> 数量）
3. 选方法：小模型用 LoRA，省显存
4. 训练：设定学习率、步数，监控 loss
5. 评估：在测试集上看是否达标
6. 部署：把微调后的权重接进你的推理服务（第 7 章）
```

## 四、什么情况下**不要**微调

- 只是想让模型“知道某份资料” → RAG 更合适、更便宜、可更新。
- 只是想固定输出格式 → 提示词 + 输出解析器（第 14 章）通常够。
- 数据量很小且噪声大 → 微调反而过拟合，不如好好写提示词。

## 五、给新手的判断口诀

> 知识不够用 RAG，格式不对调提示词，只有“模型本身能力/风格要变”才上微调。

## 结语

走到这里，你已经从“什么是大模型”走到了“能规划一次完整的 Agent 系统”。后面三个实战项目（见仓库根目录的 `实战项目-*`、`案例与源码-*`）建议挑一个亲手做一遍，知识才算真正长在自己身上。
