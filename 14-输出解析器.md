# 14 输出解析器

大模型返回的是一段文本。要让程序能用，得把它变成结构化的对象（字典、JSON、Pydantic）。本章讲 LangChain 的几种输出解析器。

## 一、StrOutputParser（最朴素）

```python
from langchain_core.output_parsers import StrOutputParser
parser = StrOutputParser()  # 原样返回字符串
```

适合只要文本的场景。

## 二、JsonOutputParser（自动结构化）

```python
from langchain_core.output_parsers import JsonOutputParser

parser = JsonOutputParser()
fmt = parser.get_format_instructions()  # 让模型按此格式输出
prompt = ChatPromptTemplate.from_messages([
    ("system", "提取订单信息。
{fmt}"),
    ("human", "{text}"),
]).partial(fmt=fmt)

chain = prompt | llm | parser
result = chain.invoke({"text": "我要买3件红色T恤，地址上海"})
print(result)  # {'items': [...], 'address': '上海'}
```

## 三、PydanticOutputParser（带类型校验，最稳）

```python
from pydantic import BaseModel, Field
from langchain_core.output_parsers import PydanticOutputParser

class Order(BaseModel):
    item: str = Field(description="商品名")
    qty: int = Field(description="数量")

parser = PydanticOutputParser(pydantic_object=Order)
# prompt 里放入 parser.get_format_instructions()
```

Pydantic 会在解析时做类型校验，模型输出不合规会抛错，方便你重试。

## 四、为什么解析器重要

- **下游能消费**：数据库、API、前端都要结构化数据，纯文本接不上。
- **可校验**：格式错了立刻发现，而不是带着脏数据往下跑。
- **少幻觉**：要求模型只填字段，减少自由发挥。

## 下一步

把“提示词→模型→解析”三步组合成可复用流程，用 [15 LCEL 与链式调用](15-LCEL与链式调用.md)。
